{
  "name": "portfolio-project",
  "version": "1.0.0",
  "description": "Personal portfolio with API documentation",
  "main": "backend/app.js",
  "scripts": {
    "start": "node backend/app.js",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
